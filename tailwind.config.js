/** @type {import('tailwindcss').Config} */
export default {
  darkMode: "class", // optional (we’re not relying on it, but safe to keep)
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: { extend: {} },
  plugins: [],
};
